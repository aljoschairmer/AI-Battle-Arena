# Drop corporate root CA files (*.crt) here before `docker build` if you are
# behind a TLS-inspecting proxy (Zscaler etc.). The directory is gitignored —
# never commit a corporate certificate to the repository.
